{
  "name": "monster_battler_spa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "DEV=true node esbuild",
    "buildProd": "node esbuild",
	"clean": "rm -r ./build",
    "watch": "WATCH=true node esbuild"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.14.41",
    "esbuild-svelte": "^0.7.0",
    "svelte": "^3.48.0"
  }
}
